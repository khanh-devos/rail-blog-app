<%= form_with(model: like), url: like_create_path(user_id: User.first.id, id: @post.id), scope: :like do |form| %>

  <div>  
    <strong>Likes:
      <%= @post.likes_counter %>
    </strong>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
