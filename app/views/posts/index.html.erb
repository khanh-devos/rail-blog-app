<p style="color: green"><%= notice %></p>

<h1>Posts & Comments</h1>

<div id="posts">
  
  <%= render @user %>

  <% @posts.each do |post| %>
    <%= link_to "/users/#{@user.id}/posts/#{post.id}" do %>
      
      <%= render post %>

    <% end %>
  <% end %>
</div>

<div class="bottom">
  <strong>
  <%= link_to "Back to all posts", "/users/#{@user.id}" %>
  </strong>
</div>
<%# <%= link_to "New post", new_post_path %>
