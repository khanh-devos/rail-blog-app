<div id="<%= dom_id user %>" >
  <div class="user_item" >
    <p>
    <% if !user.photo.blob.nil? %>
      <%= image_tag user.photo, class:'user_photo', alt:"user avatar" %>  
    <% end %>
    </p>
    
    
    <p>
      <strong>Name:</strong>
      <%= user.name %>
    </p>


    <p>
      <strong>Posts counter:</strong>
      <%= user.posts_counter %>
    </p>

  </div>


  <% if !@posts.nil? && !url_for.include?('post') %>
    <p class="bio">
      <strong><%= user.name%>'s Bio:</strong>
      <%= "#{user.bio}. " * 3 %>
    </p>

    
    <%= render @posts  %>
    
  <% end %>
  
</div>

  